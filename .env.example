# Local development example. For VPS production use .env.production.example.

APP_NAME=ProspectForge
SECRET_KEY=local-development-key-only
DEBUG=true
ENVIRONMENT=development
DATABASE_URL=sqlite+aiosqlite:///./prospectforge.db

# Local/custom ports
HTTP_PORT=18080
HTTPS_PORT=18443
APP_PORT=18081
TLS_MODE=internal
CADDYFILE_PATH=./deploy/Caddyfile.internal
FORCE_HTTPS_COOKIES=false
TRUSTED_HOSTS=*

DOMAIN=prospects.example.com
ACME_EMAIL=you@example.com

# Postgres values are used only when running docker compose.
POSTGRES_USER=prospectforge
POSTGRES_PASSWORD=change-me-db-password
POSTGRES_DB=prospectforge

# Auth
ACCESS_TOKEN_EXPIRE_MINUTES=1440
ADMIN_EMAIL=admin@prospectforge.local
ADMIN_PASSWORD=local-development-only

# Scheduler (keep OFF until live-source quality validated)
ENABLE_SCHEDULER=false
ENABLE_NIGHTLY_INGESTION=false
RUN_MIGRATIONS=true

# Discovery
INSEE_API_KEY=
SIRENE_DELAY_SECONDS=2.1
REACHER_URL=http://reacher:8080
REACHER_ENABLED=false
HARVESTER_ENABLED=false
DECP_CACHE_PATH=/app/data/decp_cache.parquet
DECP_DAYS_BACK=120
DECP_MAX_COMPANIES=150
INGESTION_RUN_CONTACTS=false
HUNTER_API_KEY=
